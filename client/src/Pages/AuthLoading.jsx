// this file should maybe be moved to a specific components file dedicated to loading states 
// when we get more states or moved for better organization

import React from "react";

export default function AuthLoading() {
    return <main>Authenticating session...</main>;
}